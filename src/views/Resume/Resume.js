import React from 'react';
import styled from 'styled-components';

const SResume = styled.main``;

const Resume = () => {
  return (
    <SResume>
      <h1>Resume</h1>
    </SResume>
  );
};

export default Resume;
